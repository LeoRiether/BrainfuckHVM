(Fix f) = (f (Fix f))

(RFactorial f 0) = 1
(RFactorial f n) = (* n (f (- n 1)))

// This fails (stuck on "Reducing")
(Factorial1 n) = ((Fix RFactorial) n)

// Also fails (stack overflow)
(LambdaFactorial )= λf λn (Rfactorial f n)
(Factorial2 n) = ((Fix LambdaFactorial) n)

// This works
(Factorial3 n) =
    let fac = λf λn (RFactorial f n)
    ((Fix fac) n)

(Main n) = (Factorial3 n)

